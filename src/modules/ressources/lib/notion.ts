// Diagnostic stub : fichier présent mais non utilisé.
// Cette PR teste si le simple fait d'avoir ce fichier dans l'arbo fait planter
// le build Vercel à l'étape "Applying modifyConfig from Vercel".

export function _diagnostic(): string {
  return "notion-orphan";
}
