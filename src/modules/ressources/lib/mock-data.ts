// Stub conservé temporairement pour ne pas casser le cache de build Vercel.
// La couche mock a été remplacée par un fetch live Notion (cf. ./notion.ts) ;
// ce fichier sera supprimé une fois le cache invalidé.
export const mockResources: never[] = [];
export const mockTemplates: never[] = [];
export const mockResourceItems: never[] = [];
