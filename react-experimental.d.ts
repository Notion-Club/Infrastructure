// Active les types React du canal expérimental (composant `<ViewTransition>` et
// ses props). Le runtime est fourni par le React compilé de Next (qui exporte
// déjà `ViewTransition`) ; côté types, ils ne vivent que dans canary/experimental.
/// <reference types="react/canary" />
/// <reference types="react/experimental" />
